{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <h1>Edit Task</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="btn-group">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a class="btn btn-warning" href="{{ path('app_task_index') }}">Annuler</a>
        {{ include('task/_delete_form.html.twig') }}
    </div>
    {{ form_end(form) }}


{% endblock %}
